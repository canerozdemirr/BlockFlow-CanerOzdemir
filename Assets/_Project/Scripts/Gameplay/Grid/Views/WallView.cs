using UnityEngine;

/// <summary>
/// Marker component for wall prefabs. Currently empty; having a named type
/// makes it easy to enumerate walls in the scene for debug gizmos, save/load
/// round-trips, and future wall-based VFX (e.g. impact ripples on collision).
/// </summary>
public sealed class WallView : MonoBehaviour
{
}
